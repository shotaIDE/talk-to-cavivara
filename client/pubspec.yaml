name: house_worker
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  riverpod:
  flutter_riverpod:
  path_provider:
  flutter_slidable:
  google_fonts:
  intl:
  firebase_core:
  firebase_auth:
  cloud_firestore:
  logging:
  package_info_plus:
  share_plus:
  url_launcher:
  freezed_annotation:
  cupertino_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  freezed:
  mockito:
  flutter_lints:

flutter:
  uses-material-design: true
